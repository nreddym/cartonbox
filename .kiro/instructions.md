# Agent Instructions

## Python Command

When running Python commands in this workspace, always use `py` instead of `python`.

Examples:
- Use `py -m pytest` instead of `python -m pytest`
- Use `py -m pip install` instead of `python -m pip install`
- Use `py script.py` instead of `python script.py`

This ensures compatibility with the Windows Python launcher.
